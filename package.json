{
  "name": "github-proxy",
  "type": "module",
  "private": true,
  "description": "GitHub proxy on Cloudflare Workers",
  "author": "mochaaP <npm@mochaa.ws> && Paperdragon <2678885646@qq.com>",
  "license": "MIT",
  "repository": "https://github.com/Paper-Dragon/github-proxy",
  "main": "dist/_worker.js",
  "scripts": {
    "build": "rimraf dist && parcel build",
    "update": "npm update -S",
    "deploy": "npm run test && rimraf dist && parcel build && wrangler pages deploy dist",
    "lint": "eslint .",
    "size": "size-limit",
    "start": "wrangler pages dev dist",
    "test": "npm run size"
  },
  "browserslist": "Firefox ESR, iOS >= 12.5, Chrome >= 87",
  "dependencies": {
    "nord": "github:nordtheme/nord",
    "ress": "^5.0.2",
    "worktop": "^0.8.0-next.18"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250327.0",
    "@mochaa/eslintrc": "0.1.12",
    "@size-limit/preset-small-lib": "11.2.0",
    "eslint": "9.22.0",
    "parcel": "2.13.3",
    "rimraf": "6.0.1",
    "size-limit": "11.2.0",
    "svgo": "3.3.2",
    "typescript": "5.7.3",
    "wrangler": "3.99.0"
  },
  "@parcel/transformer-css": {
    "drafts": {
      "nesting": true
    }
  },
  "size-limit": [
    {
      "path": "src/_worker.ts",
      "brotli": false
    }
  ],
  "targets": {
    "frontend": {
      "source": [
        "src/index.html",
        "src/404.html"
      ],
      "distDir": "dist/"
    },
    "main": {
      "source": "src/_worker.ts",
      "context": "web-worker",
      "outputFormat": "esmodule",
      "includeNodeModules": true,
      "optimize": true
    }
  }
}
